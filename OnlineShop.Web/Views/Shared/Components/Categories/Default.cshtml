@using OnlineShop.Domain.Models
@model IEnumerable<Category>

@foreach (var category in Model)
{
    <li class="nav-item" id="@category.Id">
        <a class="nav-link text-white" asp-controller="Home" asp-action="Index">@category.Name</a>
    </li>
}
